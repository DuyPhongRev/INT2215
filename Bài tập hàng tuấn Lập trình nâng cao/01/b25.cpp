#include <iostream>

using namespace std;

int main()
{
    int << a << b << c << d << e;
    if (a < b)
    {
        if (d < e)
        {

        } else swap(d, e);
    } else swap(a, b);
    return 0;
}

